<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        style="@style/DefaultPadding.DialogContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/textViewListManageDialogTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/default_padding"
            android:text="@string/list_manage"
            android:textAppearance="@style/TextAppearance.Title" />

        <EditText
            android:id="@+id/editTextListManageListTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/textViewListManageDialogTitle"
            android:hint="@string/list_title_hint"
            android:inputType="text" />
    </RelativeLayout>

    <include layout="@layout/dialog_buttons" />

</LinearLayout>