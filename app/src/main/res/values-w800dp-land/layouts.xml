<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Screens with more than 800dp of available width. This would
    include large handsets in landscape (~800dp) and
    7" and 10" devices in landscape (~960dp and ~1280dp respectively). -->

    <!-- At least 800dp wide so narrow show layout fits (at 30% of width at least 240dp). -->
    <item name="activity_overview" type="layout">@layout/activity_overview_multipane_wide</item>
    <item name="fragment_show" type="layout">@layout/fragment_show_narrow</item>

</resources>